version: '2'
services:
    donviphathanh-mongodb:
        image: mongo:4.0.2
        ports:
            - "27017:27017"
        command: mongos --configdb donviphathanh-mongodb-config
    donviphathanh-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    donviphathanh-mongodb-config:
        image: mongo:4.0.2
        container_name: donviphathanh-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
